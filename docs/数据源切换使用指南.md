# 数据源切换使用指南

## 快速开始

本系统现已支持 **Akshare** 和 **Tushare** 两种数据源，可通过配置文件灵活切换。

---

## 方案一：使用 Akshare（默认）

**优点**：
- ✅ 无需注册，开箱即用
- ✅ 无频率限制
- ✅ 数据更新快（收盘后即时）

**缺点**：
- ⚠️ 连接不稳定，经常出现 `RemoteDisconnected` 错误
- ⚠️ 爬虫数据，偶有延迟

**配置方法**：

编辑 `Input/Backtest_settings.csv`：

```csv
data_source,akshare,主数据源 (akshare/tushare)
backup_data_source,none,备用数据源 (akshare/tushare/none)
tushare_token,,Tushare API Token
data_fetch_strategy,simple,数据获取策略 (smart/simple)
```

---

## 方案二：切换到 Tushare（推荐生产环境）

**优点**：
- ✅ 官方数据，稳定可靠
- ✅ 数据质量高
- ✅ 有技术支持

**缺点**：
- ⚠️ 需要注册账号
- ⚠️ 免费用户有频率限制（200次/分钟）
- ⚠️ 数据更新稍慢（收盘后30-60分钟）

**配置步骤**：

### 1. 注册 Tushare 账号

访问：https://tushare.pro/register

### 2. 获取 Token

登录后访问：https://tushare.pro/user/token

复制您的 Token

### 3. 配置文件

编辑 `Input/Backtest_settings.csv`：

```csv
data_source,tushare,主数据源 (akshare/tushare)
backup_data_source,none,备用数据源 (akshare/tushare/none)
tushare_token,YOUR_TOKEN_HERE,Tushare API Token
data_fetch_strategy,simple,数据获取策略 (smart/simple)
```

**重要**：将 `YOUR_TOKEN_HERE` 替换为您的实际 Token

### 4. 运行回测

```bash
python3 main.py
```

---

## 方案三：智能切换模式（最佳方案）

**优点**：
- ✅ 结合两种数据源的优点
- ✅ 主数据源失败时自动切换到备用
- ✅ 最大化系统稳定性

**配置方法**：

编辑 `Input/Backtest_settings.csv`：

```csv
data_source,akshare,主数据源 (akshare/tushare)
backup_data_source,tushare,备用数据源 (akshare/tushare/none)
tushare_token,YOUR_TOKEN_HERE,Tushare API Token
data_fetch_strategy,smart,数据获取策略 (smart/simple)
```

**工作原理**：
1. 优先使用 Akshare 获取数据
2. 如果 Akshare 初始化失败，自动切换到 Tushare
3. 程序运行期间保持使用成功初始化的数据源

---

## 数据源对比

| 特性 | Akshare | Tushare |
|------|---------|---------|
| **稳定性** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **数据质量** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **免费额度** | 无限制 | 200次/分钟 |
| **注册要求** | 无 | 需要注册 |
| **实时性** | 收盘后即时 | 收盘后30-60分钟 |
| **推荐场景** | 快速测试 | 生产环境 |

---

## 常见问题

### Q1: Tushare 的 200次/分钟 够用吗？

**A**: 完全够用。对于周线回测系统：
- 假设回测 16 只股票
- 每只股票需要 2 次请求（行情+分红）
- 总共需要 32 次请求
- 远低于 200次/分钟 的限制

### Q2: 如何获取 Tushare Token？

**A**: 
1. 访问 https://tushare.pro/register 注册账号
2. 登录后访问 https://tushare.pro/user/token
3. 复制 Token 到配置文件

### Q3: 可以同时使用两种数据源吗？

**A**: 可以，使用智能切换模式（方案三）。

### Q4: 数据格式是否一致？

**A**: 是的，系统会自动标准化两种数据源的输出格式，确保完全一致。

### Q5: 如何验证数据源是否正常工作？

**A**: 查看日志输出，会显示当前使用的数据源：

```
✅ 数据源初始化: akshare
```

或

```
✅ 数据源初始化: tushare
```

### Q6: 如果 Tushare Token 无效会怎样？

**A**: 系统会报错并提示：

```
ValueError: 使用 Tushare 数据源需要提供 tushare_token
```

请检查 Token 是否正确填写。

---

## 环境变量配置（可选）

如果不想在配置文件中明文保存 Token，可以使用环境变量：

### macOS/Linux

```bash
# 临时设置（当前终端会话）
export TUSHARE_TOKEN="your_token_here"

# 永久设置（添加到 ~/.zshrc 或 ~/.bashrc）
echo 'export TUSHARE_TOKEN="your_token_here"' >> ~/.zshrc
source ~/.zshrc
```

### Windows

```cmd
# 临时设置
set TUSHARE_TOKEN=your_token_here

# 永久设置（系统环境变量）
setx TUSHARE_TOKEN "your_token_here"
```

然后在配置文件中留空 `tushare_token`：

```csv
tushare_token,,Tushare API Token
```

系统会自动从环境变量读取。

---

## 配置示例

### 示例 1：仅使用 Akshare

```csv
data_source,akshare
backup_data_source,none
tushare_token,
data_fetch_strategy,simple
```

### 示例 2：仅使用 Tushare

```csv
data_source,tushare
backup_data_source,none
tushare_token,YOUR_TOKEN_HERE
data_fetch_strategy,simple
```

### 示例 3：Akshare 主 + Tushare 备用（推荐）

```csv
data_source,akshare
backup_data_source,tushare
tushare_token,YOUR_TOKEN_HERE
data_fetch_strategy,smart
```

### 示例 4：Tushare 主 + Akshare 备用

```csv
data_source,tushare
backup_data_source,akshare
tushare_token,YOUR_TOKEN_HERE
data_fetch_strategy,smart
```

---

## 技术支持

如有问题，请查看：
- **Tushare 官方文档**：https://tushare.pro/document/2
- **Akshare 官方文档**：https://akshare.akfamily.xyz/

---

## 更新日志

**v1.0 (2025-12-12)**
- ✅ 实现 Akshare 和 Tushare 数据源切换
- ✅ 支持智能降级模式
- ✅ 完全向后兼容
- ✅ 数据格式自动标准化

---

**祝您回测顺利！** 🚀
